using System;

class DifferenceBetweenCSharpAndDotNetFramework
{
    static void Main(string[] args)
    {
        Console.WriteLine("Describe the difference between C# and .NET Framework.");
        Console.WriteLine();
        Console.WriteLine("Answer:");
        Console.WriteLine("C# is a language, .NET is an application framework. The .NET libraries can run  on the CLR and thus any language which can run on the CLR can also use the .NET libraries.");
    }
}

